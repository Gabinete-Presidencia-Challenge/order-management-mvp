name: Orders Service — CI

on:
  push:
    paths:
      - "services/orders-service/**"
      - ".github/workflows/orders-service-ci.yml"
  pull_request:
    paths:
      - "services/orders-service/**"

jobs:
  test:
    name: Unit tests
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: services/orders-service

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: services/orders-service/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run tests with coverage
        run: |
          pytest app/tests/ -v --tb=short \
            --junitxml=test-results.xml

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: orders-service-test-results
          path: services/orders-service/test-results.xml

  lint:
    name: Lint
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: services/orders-service

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install ruff
        run: pip install ruff

      - name: Run ruff
        run: ruff check app/

  build:
    name: Docker build check
    runs-on: ubuntu-latest
    needs: [test, lint]

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          docker build \
            -t orders-service:ci \
            ./services/orders-service
