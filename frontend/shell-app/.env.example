# Copy this file to .env and adjust for your environment.
# This file is safe to commit. .env is NOT.

# When running locally without Docker:
# USERS_API_URL=http://localhost:8002/api/v1
# ORDERS_MFE_URL=http://localhost:3001

# When running via Docker Compose (these are the defaults — .env not required):
USERS_API_URL=http://localhost:8080/api/users/v1
ORDERS_MFE_URL=http://localhost:8080
