# Copy this file to .env and adjust for your environment.
# This file is safe to commit. .env is NOT.

# When running locally without Docker:
# ORDERS_API_URL=http://localhost:8001/api/v1

# When running via Docker Compose (this is the default — .env not required):
ORDERS_API_URL=http://localhost:8080/api/orders/v1
